---
title: '지원하는 모델'
description: 'DroidRun은 **LLM에 구애받지 않습니다(LLM agnostic)**. 즉, 존재하는 모든 LLM 프로바이더를 사용할 수 있습니다.
내부적으로 DroidRun은 [LlamaIndex LLM 프로바이더](https://docs.llamaindex.ai/en/stable/api_reference/llms/)를 활용하여 모든 주요 언어 모델 제공자와 원활하게 통합됩니다. 이는 필요에 따라 다른 프로바이더로 쉽게 전환하거나 여러 프로바이더를 동시에 사용할 수 있음을 의미합니다.
'
---

> 📖 **LLM agnostic이란?**
> 
> 특정 언어 모델(LLM) 제공자나 모델에 종속되지 않는다는 의미입니다. 
> 
> **쉬운 비유**: 멀티탭처럼 어떤 전자기기든 꽂아서 쓸 수 있는 것과 같습니다. 삼성 플러그든 애플 플러그든 상관없이 모두 사용 가능합니다.
> 
> **실용적 장점**:
> - OpenAI의 GPT를 쓰다가 Claude로 바꾸기가 쉬움
> - 여러 모델을 비교 테스트 가능
> - 특정 업체에 락인(lock-in)되지 않음

## LLM 프로바이더 설치

```bash
# 선호하는 LLM 프로바이더 패키지 설치
pip install llama-index-llms-[PROVIDER]
```

> 💡 **[PROVIDER] 부분은 실제 프로바이더 이름으로 대체하세요**
> 
> 예시:
> - OpenAI 사용 시: `pip install llama-index-llms-openai`
> - Anthropic 사용 시: `pip install llama-index-llms-anthropic`
> - Google 사용 시: `pip install llama-index-llms-google`

## 지원하는 프로바이더

DroidRun은 LlamaIndex를 통해 모든 주요 LLM 프로바이더를 지원합니다:
- **OpenAI** (GPT-4o, GPT-o3 등)
- **Anthropic** (Claude 모델들)
- **GoogleGenAI** (Gemini)
- **OpenAILike** (OpenAI 호환 API - OpenRouter, Azure OpenAI, 로컬 LM Studio 등)
- **DeepSeek** (DeepSeek LLM 통합)
- **Azure OpenAI Service**
- **AWS Bedrock**
- **Cohere**
- **Hugging Face**
- **로컬 모델** (Ollama, vLLM 등)
- 그 외 다수...

> 📖 **OpenAI-compatible APIs란?**
> 
> OpenAI가 정의한 API 규격(인터페이스)을 따르는 서비스들을 의미합니다.
> 
> **실용적 예시**:
> - **OpenRouter**: 여러 LLM을 하나의 API로 사용할 수 있는 통합 게이트웨이
> - **Azure OpenAI**: 마이크로소프트 클라우드에서 제공하는 OpenAI 모델
> - **LM Studio**: 내 컴퓨터에서 로컬로 돌리는 오픈소스 모델을 OpenAI API 형식으로 사용
> 
> **장점**: OpenAI용으로 작성한 코드를 거의 그대로 다른 서비스에서도 사용 가능

이러한 유연성 덕분에 특정 사용 사례, 예산, 성능 요구사항에 가장 적합한 모델을 선택할 수 있으며, 모든 프로바이더에서 동일한 DroidRun 인터페이스를 유지할 수 있습니다.

> 💡 **실무 팁**:
> - 개발 초기: 저렴한 모델(GPT-3.5, Claude Haiku)로 프로토타입 제작
> - 성능 중요: 최신 모델(GPT-4o, Claude Opus)로 전환
> - 비용 절감: 로컬 모델(Ollama)이나 오픈소스 모델 활용
> - 보안 중요: 온프레미스 Azure OpenAI나 AWS Bedrock 사용